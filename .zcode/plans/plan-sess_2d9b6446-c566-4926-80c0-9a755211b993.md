# Plan: Mejoras Premium — Glassmorphism, Animaciones GSAP y Nuevas Secciones UI

## Archivos a modificar:
1. **`styles.css`** — Mejoras visuales CSS (glassmorphism, scrollbar, skeleton loading, wave divider, testimonios, floating buttons, badges animados)
2. **`index.html`** — Nuevas secciones (testimonios, wave divider, floating buttons)
3. **`script.js`** — Animaciones GSAP avanzadas (parallax secciones, stagger cards, tabs animadas, testimonios autoplay, smooth reveal)
4. **`admin.css`** — Glassmorphism mejorado en admin (cards, modales, inputs)
5. **`admin.js`** — Transiciones animadas en tabs del admin

---

## 1. 💎 Glassmorphism + Tema Dark Premium

### 1a. Product Cards — Glassmorphism mejorado (`styles.css`)
- Aumentar `backdrop-filter: blur()` de 16px → 24px en `.product-card`
- Agregar borde con gradiente sutil: `border-image: linear-gradient(135deg, rgba(0,240,255,.12), rgba(138,43,226,.08)) 1`
- Sombra interna más pronunciada y glow externo más intenso al hover
- Nuevo efecto: **border gradient animado rotativo** al hover usando `@property` + `conic-gradient`
- Imagen con `overflow: hidden` + `mask-image: linear-gradient(to bottom, black 80%, transparent)` para fade suave

### 1b. Modales — Frosted glass premium (`styles.css`)
- Aumentar `backdrop-filter` en `.modal-overlay` a `blur(20px)`
- Agregar borde glow cyan tenue al modal content
- `.modal-content` con gradiente de fondo sutil en vez de color plano

### 1c. Scrollbar personalizado (`styles.css`)
- `::-webkit-scrollbar` con width 8px, track oscuro, thumb con gradiente cyan→púrpura, border-radius
- Scrollbar en Firefox con `scrollbar-width: thin; scrollbar-color`

### 1d. Admin — Glassmorphism (`admin.css`)
- `.admin-modal-content` con `backdrop-filter: blur(24px)` y fondo semi-transparente
- `.login-card` con glassmorphism más fuerte
- `.table-container` con blur sutil
- Inputs del admin con glow border on focus consistente

---

## 2. 🚀 Animaciones GSAP/JS Avanzadas

### 2a. Stagger scroll reveal en Product Cards (`script.js`)
- Reemplazar la lógica de `reveal-up` en `renderPage()` con GSAP ScrollTrigger
- Cada batch de 12 cards aparece con stagger de 0.08s (fade-up + scale)
- Usar `ScrollTrigger.batch('.product-card', { onEnter: stagger animation })`

### 2b. Parallax en secciones enteras (`script.js`)
- Hero orbs: animar con `ScrollTrigger` para que se muevan a diferentes velocidades (ya tienen `data-speed`)
- Outline text section: parallax horizontal sutil al scroll
- Stats section: fade-in con stagger cuando entra al viewport
- Carousel section: parallax ligero en el wrapper

### 2c. Tabs del admin con transición animada (`admin.js` + `admin.css`)
- En vez de `display: none/flex`, usar `opacity: 0; transform: translateY(15px); position: absolute` para ocultar
- Al activar: fade-in + slide-up con CSS transition
- Agregar indicador deslizante bottom que se mueve a la tab activa

### 2d. Product card hover mejorado (`script.js`)
- Aumentar intensidad del 3D tilt (de 4deg a 6deg)
- Agregar spotlight radial que sigue al mouse dentro de la card

---

## 3. ✨ Nuevas Secciones y Elementos UI

### 3a. Sección de Testimonios/Reviews (`index.html` + `styles.css` + `script.js`)
- Nueva sección antes del footer con carrusel de testimonios
- Cards tipo quote con avatar, nombre, estrellas, comentario
- Autoplay con GSAP (slide cada 5s)
- Datos: reviews reales desde Firebase (ya existen en la DB)
- Diseño: cards glassmorphism con borde cyan tenue, comillas decorativas grandes

### 3b. Wave divider en footer (`styles.css` + `index.html`)
- SVG wave animado como separador entre products y footer
- Usar SVG inline con path wave y animación sutil

### 3c. Botón "Volver arriba" (`index.html` + `styles.css` + `script.js`)
- Fijo bottom-right, aparece después de scrollear 400px
- Diseño: botón circular cyan con icono flecha, glow
- Animación: fade-in al aparecer, hover scale + glow boost
- Click: smooth scroll al top

### 3d. WhatsApp flotante (`index.html` + `styles.css` + `script.js`)
- Fijo bottom-left con icono WhatsApp
- Animación bounce de entrada al cargar
- Pulso sutil cada 3s para atraer atención
- Link directo a `wa.me/542634478522`

### 3e. Badge "Nuevo" con pulso animado (`styles.css`)
- Nuevo estilo `.badge-new` para productos agregados recientemente
- Animación de pulso (scale + glow) cada 2s
- Se aplicaría si el producto tiene `badge: "Nuevo"` (campo existente)

### 3f. Skeleton loading (`styles.css` + `script.js`)
- mientras carga Firebase, mostrar 12 skeleton cards con gradiente shimmer animado
- Reemplazar el grid vacío inicial con skeletons